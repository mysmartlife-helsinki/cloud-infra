OK Test connectivity to machines, git repo, docs 
OK Inspect delivered VMs  
OK  Describe a list of requirements, and good to have. Discuss if is the case to modify delivered VMs

   OK db is missing sdb disk
   SKIP data disks are 10GB. we talked about 40GB. 10 GB is OK, but is not very much. 

OK  Feedback loop on the above

OK  Create partition: XFS FS with mount options: 'rw,noatime,nobarrier,discard'
-  Tune OS kernel parameters, avoid OOM killer
-  
OK  Install Postgres instance
OK  Initialize db
OK  Install and create extension PostGis
-  Check firewalling, adjust it based on networking and sibilings
OK  Harden: no listen on external inerface
-  Configure and Tune Postgres instance
     - commit config files!

-  Evaluate additional extensions
-  Create Siblings
-  Configure master and siblings, instruct replication, pg_hba.conf with separate users
-  Document failover procedure
-  Document extension creation: per db, how, which postgis_* is needed 
-  Discuss about:
    - The need of pgbouncer 
    - When and how to failover
    - The need to script the creation of a sibiling
    - The need to have sync or async replication
    - Backups, PITR
    - Monitoring and alerting
    - Graphing

-  Test Reboot 
-  Create banchmark baselines?

